# PuniCodex — 3-Day SEO Campaign Report

**Period:** 2026-08-01 → 2026-08-03 · three scheduled waves + intervening audit-response waves
**Scope:** sitewide organic discovery — SERP discipline, structured data, internal linking, performance
**Site:** 924+ pages · 271 flagship temples · 1,084 blog dispatches · sitemap ~6,965 URLs

---

## Executive summary

Three waves took the site from "rich but machine-thin" to a fully machine-readable estate:
every page now carries disciplined titles/descriptions, four schema families are deployed
(WebPage/Thing, BlogPosting, CollectionPage+ItemList, VideoGame, BreadcrumbList, FAQ),
internal-link depth reaches the archive and pattern layers, and the full validation
battery — links, SEO markup, idempotency, lighthouse — is green end-to-end.

## Wave 1 — SERP discipline (day 1)

Baseline audit found **49 SERP issues in 5 classes** (duplicate/weak titles, thin
descriptions, inconsistent series metadata).

- Flagship title + description discipline: **271/271 temples pass** (0 issues after fix).
- Base temples, hand pages, /patterns/, store, /texts/ titles normalized; blog
  `serpTitle` discipline applied to all series (each 120–160 chars, unique).
- FAQ schema shipped on /rulebook/ and /patterns/.
- Store regression fixed alongside: **3,531 product mockups** restored and protected
  by a preservation rule in the product generator (keeps `mockupImage` +
  `variantPricing` across regenerations).
- Edge cases closed: 0 description issues remaining sitewide.

## Wave 2 — performance + internal linking (day 2)

- **Lighthouse: EXIT 0** — the 90+ desktop bar held across the 18-page sample
  (incl. /blog/, /cards/, /game/); no perf regressions to fix this wave.
- **"From the Archive"** section added to the homepage: three pantheon-diverse
  Restoration Files cards (Greek/Norse/Nahuatl) + link to the full 1,084-dispatch
  archive — new internal-link depth into the blog series.
- **VideoGame JSON-LD** on /game/ (free-to-play, single-player, browser platform)
  with a click-selling description + og:image.
- **CollectionPage JSON-LD** on /cards/ (1,698 items, edition breakdown).
- Gates: **697,257 links valid · 924/924 SEO pages · blog-series ✓**.

## Wave 3 — structured-data expansion (day 3)

- **BreadcrumbList JSON-LD fleet-wide** — Home → Pantheon → Temple → Tab, absolute
  URLs, generated at the template level for every flagship page type
  (index, lore, extended lore, gallery, patterns, scholars, creatives, patron,
  dashboard, blog tab + all three series: Restoration/Resonance/Canonical).
  Coverage: **271 temples × 12 page types ≈ 3,250+ pages**. Verified: 7/7 sample
  pages parse with correct positions and absolute URLs.
- **ItemList JSON-LD** (first 50 entries, absolute URLs) baked into **/lexicon/**
  and **/pantheon/**, regenerable via `scripts/generate-itemlist-schemas.js`
  (pipeline-registered).
- Final battery: **924/924 SEO pages · 697,188 links valid · blog-series ✓ ·
  mythic-duel-v2 ✓ · lighthouse EXIT 0**.

## Also landed between waves (audit-response waves)

- **/cards/ went from an empty JS shell to 1,698 static, crawlable frames**
  (server-rendered grid + payload-first hydration) — the most link-worthy surface
  on the site is now fully indexable.
- Rulebook dual-tier pairs, tier-row examples, Átlās exception, Athēnâ note,
  IDNA precision, and a visible last-updated date — the doctrine page is now
  internally consistent.
- Loot-box compliance: published pack odds + a deterministic Archive Exchange path.
- Keyword vocabulary re-aligned to the brand (Invocation / Katabasis / Epithet /
  Syzygy / Ink Tide).

## Current SEO posture

| Surface | State |
|---|---|
| Titles & descriptions | Disciplined, unique, 0 known issues (924 pages) |
| Schema | WebPage/Thing, BlogPosting, CollectionPage, ItemList, VideoGame, BreadcrumbList, FAQ — all parsing |
| Internal linking | Homepage → all major sections; archive + pattern layers reachable within 2 clicks |
| Crawlability | Cards + store + texts fully static; sitemap clean (6,965 URLs, no orphans, no interstitial) |
| Performance | Lighthouse 90+ desktop held; immutable static caching with version-busted assets |
| Canonicals | Absolute everywhere; media proxy offloads temple media to the masters host |

**Remaining known items:** temple hero videos remain heavy (future compression pass);
the `temple-assets/` payload restructure would slim deployments (not an SEO concern);
two cron-scheduled SEO review waves can repeat this process quarterly.

*Generated by the wave 3 cron run, 2026-08-03.*
