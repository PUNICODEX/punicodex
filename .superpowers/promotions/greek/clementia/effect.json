{
  "canvasId": "clementia-canvas",
  "label": "Gentle Rain of Mercy",
  "description": "Soft olive branches, gentle light, and slow rain for the Roman personification of mercy and forgiveness.",
  "code": "const drops = [];\nconst branches = [];\nfor (let i = 0; i < 60; i++) drops.push({ x: Math.random(), y: Math.random(), len: 10 + Math.random() * 15, speed: 0.0003 + Math.random() * 0.0003 });\nfor (let i = 0; i < 8; i++) branches.push({ x: Math.random(), y: Math.random(), a: Math.random() * Math.PI * 2, len: 60 + Math.random() * 80 });\nlet t = 0;\nfunction draw() {\n  if (reduced) return;\n  ctx.clearRect(0, 0, width, height);\n  const g = ctx.createRadialGradient(width * 0.5, height * 0.5, 0, width * 0.5, height * 0.5, Math.max(width, height) * 0.55);\n  g.addColorStop(0, 'rgba(122,154,122,0.12)');\n  g.addColorStop(1, 'rgba(8,10,8,0)');\n  ctx.fillStyle = g;\n  ctx.fillRect(0, 0, width, height);\n  ctx.globalAlpha = 0.25;\n  ctx.strokeStyle = '#7a9a7a';\n  ctx.lineWidth = 1.5;\n  for (const b of branches) {\n    const x = b.x * width, y = b.y * height;\n    ctx.save();\n    ctx.translate(x, y);\n    ctx.rotate(b.a + Math.sin(t * 0.001 + b.x * 10) * 0.05);\n    ctx.beginPath();\n    ctx.moveTo(-b.len * 0.5, 0);\n    ctx.quadraticCurveTo(0, -b.len * 0.2, b.len * 0.5, 0);\n    ctx.stroke();\n    for (let j = -3; j <= 3; j++) {\n      ctx.beginPath();\n      ctx.ellipse(j * b.len * 0.12, -Math.abs(j) * 2, 4, 2, 0, 0, Math.PI * 2);\n      ctx.stroke();\n    }\n    ctx.restore();\n  }\n  ctx.globalAlpha = 0.4;\n  ctx.strokeStyle = '#a8c4a8';\n  ctx.lineWidth = 1;\n  for (const d of drops) {\n    const y = (d.y + d.speed * t) % 1;\n    ctx.beginPath();\n    ctx.moveTo(d.x * width, y * height);\n    ctx.lineTo(d.x * width, y * height + d.len);\n    ctx.stroke();\n  }\n  ctx.globalAlpha = 1;\n  t++;\n  requestAnimationFrame(draw);\n}\ndraw();"
}
