{
  "canvasId": "arete-canvas",
  "label": "Golden Excellence",
  "description": "Drifting laurel leaves and slow golden rays for the Greek personification of excellence and virtue.",
  "code": "const leaves = [];\nfor (let i = 0; i < 45; i++) {\n  leaves.push({ x: Math.random(), y: Math.random(), r: 8 + Math.random() * 14, a: Math.random() * Math.PI * 2, drift: Math.random(), speed: 0.0001 + Math.random() * 0.0002 });\n}\nlet t = 0;\nfunction draw() {\n  if (reduced) return;\n  ctx.clearRect(0, 0, width, height);\n  const g = ctx.createRadialGradient(width * 0.5, height * 0.35, 0, width * 0.5, height * 0.35, Math.max(width, height) * 0.6);\n  g.addColorStop(0, '#2a2210');\n  g.addColorStop(1, '#0a0804');\n  ctx.fillStyle = g;\n  ctx.fillRect(0, 0, width, height);\n  ctx.globalAlpha = 0.15;\n  ctx.strokeStyle = '#d4af37';\n  ctx.lineWidth = 1;\n  const cx = width * 0.5, cy = height * 0.35;\n  for (let i = 0; i < 16; i++) {\n    const a = (i / 16) * Math.PI * 2 + t * 0.0003;\n    ctx.beginPath();\n    ctx.moveTo(cx, cy);\n    ctx.lineTo(cx + Math.cos(a) * width * 0.5, cy + Math.sin(a) * height * 0.5);\n    ctx.stroke();\n  }\n  ctx.globalAlpha = 0.5;\n  ctx.strokeStyle = '#b8972e';\n  for (const f of leaves) {\n    const x = (f.x + Math.sin(t * 0.0005 + f.drift) * 0.03) * width;\n    const y = ((f.y + t * f.speed) % 1.1 - 0.05) * height;\n    ctx.save();\n    ctx.translate(x, y);\n    ctx.rotate(f.a + t * 0.001);\n    ctx.beginPath();\n    ctx.ellipse(0, 0, f.r, f.r * 0.45, 0, 0, Math.PI * 2);\n    ctx.stroke();\n    ctx.restore();\n  }\n  ctx.globalAlpha = 1;\n  t++;\n  requestAnimationFrame(draw);\n}\ndraw();"
}
