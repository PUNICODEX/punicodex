{
  "canvasId": "anima-canvas",
  "label": "Breathing Soul",
  "description": "Soft drifting particles and a slow breathing glow for the Latin life-force, soul, and breath.",
  "code": "const particles = [];\nfor (let i = 0; i < 100; i++) {\n  particles.push({ x: Math.random(), y: Math.random(), r: 0.5 + Math.random() * 2, vy: -0.0001 - Math.random() * 0.0002, vx: (Math.random() - 0.5) * 0.0001, phase: Math.random() * Math.PI * 2 });\n}\nlet t = 0;\nfunction draw() {\n  if (reduced) return;\n  ctx.clearRect(0, 0, width, height);\n  const breath = 0.5 + 0.5 * Math.sin(t * 0.02);\n  const g = ctx.createRadialGradient(width * 0.5, height * 0.55, 0, width * 0.5, height * 0.55, Math.max(width, height) * 0.5);\n  g.addColorStop(0, 'rgba(107,140,174,' + (0.12 * breath) + ')');\n  g.addColorStop(1, 'rgba(10,12,16,0)');\n  ctx.fillStyle = g;\n  ctx.fillRect(0, 0, width, height);\n  ctx.globalAlpha = 0.6;\n  for (const p of particles) {\n    let y = (p.y + p.vy * t + Math.sin(t * 0.005 + p.phase) * 0.02) % 1;\n    if (y < 0) y += 1;\n    let x = (p.x + p.vx * t + Math.cos(t * 0.003 + p.phase) * 0.01) % 1;\n    if (x < 0) x += 1;\n    ctx.fillStyle = '#c9dce8';\n    ctx.beginPath();\n    ctx.arc(x * width, y * height, p.r * (0.8 + 0.4 * breath), 0, Math.PI * 2);\n    ctx.fill();\n  }\n  ctx.globalAlpha = 1;\n  t++;\n  requestAnimationFrame(draw);\n}\ndraw();"
}
