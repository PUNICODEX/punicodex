{
  "canvasId": "animus-canvas",
  "label": "Inner Fire",
  "description": "A pulsing core of amber flame and rising sparks for the Latin mind, spirit, and courage.",
  "code": "const sparks = [];\nfor (let i = 0; i < 70; i++) {\n  sparks.push({ x: Math.random(), y: Math.random(), r: 0.5 + Math.random() * 1.5, vy: -0.0002 - Math.random() * 0.0003, life: Math.random(), phase: Math.random() * Math.PI * 2 });\n}\nlet t = 0;\nfunction draw() {\n  if (reduced) return;\n  ctx.clearRect(0, 0, width, height);\n  const pulse = 0.6 + 0.4 * Math.sin(t * 0.03);\n  const g = ctx.createRadialGradient(width * 0.5, height * 0.6, 0, width * 0.5, height * 0.6, Math.max(width, height) * 0.45);\n  g.addColorStop(0, 'rgba(184,115,51,' + (0.2 * pulse) + ')');\n  g.addColorStop(1, 'rgba(12,8,4,0)');\n  ctx.fillStyle = g;\n  ctx.fillRect(0, 0, width, height);\n  ctx.globalAlpha = 0.7;\n  for (const s of sparks) {\n    let y = (s.y + s.vy * t) % 1;\n    if (y < 0) y += 1;\n    const x = s.x + Math.sin(t * 0.004 + s.phase) * 0.02;\n    const a = Math.max(0, Math.sin(s.life * Math.PI + t * 0.01));\n    ctx.fillStyle = a > 0.5 ? '#ffcc80' : '#b87333';\n    ctx.beginPath();\n    ctx.arc(x * width, y * height, s.r * (0.7 + 0.6 * pulse), 0, Math.PI * 2);\n    ctx.fill();\n  }\n  ctx.globalAlpha = 1;\n  t++;\n  requestAnimationFrame(draw);\n}\ndraw();"
}
