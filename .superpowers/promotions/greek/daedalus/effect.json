{
  "canvasId": "daedalus-canvas",
  "label": "Wings of the Craftsman",
  "description": "Falling feathers, turning gears, and bronze sparks for the inventor who flew too close and too well.",
  "code": "const feathers = [];\nconst gears = [];\nfor (let i = 0; i < 40; i++) feathers.push({ x: Math.random(), y: Math.random(), r: 6 + Math.random() * 10, a: Math.random() * Math.PI * 2, speed: 0.0002 + Math.random() * 0.0002, sway: Math.random() });\nfor (let i = 0; i < 5; i++) gears.push({ x: 0.2 + Math.random() * 0.6, y: 0.2 + Math.random() * 0.6, r: 20 + Math.random() * 40, teeth: 8 + Math.floor(Math.random() * 8), dir: Math.random() > 0.5 ? 1 : -1 });\nlet t = 0;\nfunction draw() {\n  if (reduced) return;\n  ctx.clearRect(0, 0, width, height);\n  const g = ctx.createLinearGradient(0, 0, 0, height);\n  g.addColorStop(0, '#1a1408');\n  g.addColorStop(1, '#0a0804');\n  ctx.fillStyle = g;\n  ctx.fillRect(0, 0, width, height);\n  ctx.globalAlpha = 0.25;\n  ctx.strokeStyle = '#8a6f2a';\n  ctx.lineWidth = 1.5;\n  for (const gr of gears) {\n    const cx = gr.x * width, cy = gr.y * height;\n    ctx.save();\n    ctx.translate(cx, cy);\n    ctx.rotate(t * 0.002 * gr.dir);\n    ctx.beginPath();\n    for (let j = 0; j < gr.teeth * 2; j++) {\n      const a = (j / (gr.teeth * 2)) * Math.PI * 2;\n      const r = j % 2 === 0 ? gr.r : gr.r * 0.75;\n      ctx.lineTo(Math.cos(a) * r, Math.sin(a) * r);\n    }\n    ctx.closePath();\n    ctx.stroke();\n    ctx.beginPath();\n    ctx.arc(0, 0, gr.r * 0.25, 0, Math.PI * 2);\n    ctx.stroke();\n    ctx.restore();\n  }\n  ctx.globalAlpha = 0.6;\n  ctx.strokeStyle = '#d4b85a';\n  ctx.lineWidth = 1;\n  for (const f of feathers) {\n    const x = (f.x + Math.sin(t * 0.0004 + f.sway * 10) * 0.02) * width;\n    const y = ((f.y + f.speed * t) % 1.1 - 0.05) * height;\n    ctx.save();\n    ctx.translate(x, y);\n    ctx.rotate(f.a + t * 0.001);\n    ctx.beginPath();\n    ctx.moveTo(-f.r, 0);\n    ctx.quadraticCurveTo(0, -f.r * 0.5, f.r, 0);\n    ctx.quadraticCurveTo(0, f.r * 0.5, -f.r, 0);\n    ctx.stroke();\n    ctx.restore();\n  }\n  ctx.globalAlpha = 1;\n  t++;\n  requestAnimationFrame(draw);\n}\ndraw();"
}
