{
  "canvasId": "ajax-canvas",
  "label": "Bronze Bulwark",
  "description": "Drifting shield-wall particles, bronze sparks, and slow ash for the warrior who held the Achaean line.",
  "code": "const particles = [];\nfor (let i = 0; i < 90; i++) {\n  particles.push({ x: Math.random(), y: Math.random(), r: 0.5 + Math.random() * 1.5, vx: (Math.random() - 0.5) * 0.0003, vy: (Math.random() - 0.5) * 0.0003, life: Math.random() });\n}\nlet t = 0;\nfunction draw() {\n  if (reduced) return;\n  ctx.clearRect(0, 0, width, height);\n  const g = ctx.createRadialGradient(width * 0.5, height * 0.45, 0, width * 0.5, height * 0.45, Math.max(width, height) * 0.6);\n  g.addColorStop(0, '#1a120a');\n  g.addColorStop(1, '#0a0805');\n  ctx.fillStyle = g;\n  ctx.fillRect(0, 0, width, height);\n  ctx.globalAlpha = 0.25;\n  ctx.strokeStyle = '#8B6914';\n  ctx.lineWidth = 1;\n  for (let i = 0; i < 6; i++) {\n    const y = height * (0.3 + i * 0.08) + Math.sin(t * 0.002 + i) * 20;\n    ctx.beginPath();\n    ctx.moveTo(0, y);\n    ctx.lineTo(width, y);\n    ctx.stroke();\n  }\n  ctx.globalAlpha = 0.8;\n  for (const p of particles) {\n    let x = (p.x + p.vx * t) % 1;\n    if (x < 0) x += 1;\n    let y = (p.y + p.vy * t) % 1;\n    if (y < 0) y += 1;\n    ctx.fillStyle = p.life > 0.7 ? '#d4a85a' : '#8B6914';\n    ctx.beginPath();\n    ctx.arc(x * width, y * height, p.r, 0, Math.PI * 2);\n    ctx.fill();\n  }\n  ctx.globalAlpha = 0.12;\n  ctx.fillStyle = '#5a4a2a';\n  ctx.beginPath();\n  ctx.arc(width * 0.5 + Math.sin(t * 0.001) * 80, height * 0.45, 120 + Math.sin(t * 0.003) * 30, 0, Math.PI * 2);\n  ctx.fill();\n  ctx.globalAlpha = 1;\n  t++;\n  requestAnimationFrame(draw);\n}\ndraw();"
}
