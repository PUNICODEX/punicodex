{
  "canvasId": "humbaba-canvas",
  "label": "Cedar Forest Storm",
  "description": "Dark cedars, wind-borne needles, and lightning flashes for the guardian of the Amanus range.",
  "code": "const trees = [];\nfor (let i = 0; i < 12; i++) {\n  trees.push({ x: (i / 11) * 1.1 - 0.05, w: 0.04 + Math.random() * 0.03, h: 0.4 + Math.random() * 0.35 });\n}\nconst needles = [];\nfor (let i = 0; i < 80; i++) {\n  needles.push({ x: Math.random(), y: Math.random(), s: 0.5 + Math.random(), a: Math.random() * Math.PI * 2 });\n}\nlet t = 0;\nfunction draw() {\n  if (reduced) return;\n  ctx.clearRect(0, 0, width, height);\n  const g = ctx.createLinearGradient(0, 0, 0, height);\n  g.addColorStop(0, '#0a1014');\n  g.addColorStop(0.5, '#142018');\n  g.addColorStop(1, '#1e2a1c');\n  ctx.fillStyle = g;\n  ctx.fillRect(0, 0, width, height);\n  ctx.globalAlpha = 0.6;\n  ctx.fillStyle = '#1a3020';\n  for (const tr of trees) {\n    const tx = tr.x * width;\n    const th = tr.h * height;\n    ctx.beginPath();\n    ctx.moveTo(tx, height);\n    ctx.lineTo(tx + tr.w * width * 0.5, height - th);\n    ctx.lineTo(tx + tr.w * width, height);\n    ctx.closePath();\n    ctx.fill();\n  }\n  ctx.globalAlpha = 0.3;\n  ctx.fillStyle = '#4a6a40';\n  for (const n of needles) {\n    const nx = (n.x + Math.sin(t * 0.0005 + n.a) * 0.03) * width;\n    const ny = (n.y + t * 0.0002 * n.s) % 1.1 * height - 20;\n    ctx.save();\n    ctx.translate(nx, ny);\n    ctx.rotate(n.a + t * 0.002);\n    ctx.fillRect(-n.s, -n.s * 3, n.s * 2, n.s * 6);\n    ctx.restore();\n  }\n  if (Math.random() > 0.97) {\n    ctx.globalAlpha = 0.5;\n    ctx.fillStyle = '#c8d8c0';\n    ctx.fillRect(0, 0, width, height);\n  }\n  ctx.globalAlpha = 0.2;\n  const glow = ctx.createRadialGradient(width * 0.5, height * 0.3, 0, width * 0.5, height * 0.3, width * 0.5);\n  glow.addColorStop(0, 'rgba(200, 216, 192, 0.15)');\n  glow.addColorStop(1, 'rgba(200, 216, 192, 0)');\n  ctx.fillStyle = glow;\n  ctx.beginPath();\n  ctx.arc(width * 0.5, height * 0.3, width * 0.5, 0, Math.PI * 2);\n  ctx.fill();\n  ctx.globalAlpha = 1;\n  t++;\n  requestAnimationFrame(draw);\n}\ndraw();"
}
