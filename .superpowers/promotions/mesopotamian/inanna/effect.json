{
  "canvasId": "inanna-canvas",
  "label": "Venus Rising",
  "description": "A dawn sky with the eight-pointed star of Venus, drifting rosettes, and subtle lion silhouettes for the Lady of Heaven.",
  "code": "const stars = [];\nfor (let i = 0; i < 80; i++) {\n  stars.push({ x: Math.random(), y: Math.random(), r: 0.3 + Math.random() * 1.2, blink: Math.random() * Math.PI * 2 });\n}\nconst rosettes = [];\nfor (let i = 0; i < 12; i++) {\n  rosettes.push({ x: Math.random(), y: Math.random(), s: 0.5 + Math.random() * 0.8, a: Math.random() * Math.PI * 2 });\n}\nlet t = 0;\nfunction drawStar(cx, cy, r, points) {\n  ctx.beginPath();\n  for (let i = 0; i < points * 2; i++) {\n    const radius = i % 2 === 0 ? r : r * 0.4;\n    const a = (i / (points * 2)) * Math.PI * 2 - Math.PI / 2;\n    const x = cx + Math.cos(a) * radius;\n    const y = cy + Math.sin(a) * radius;\n    if (i === 0) ctx.moveTo(x, y); else ctx.lineTo(x, y);\n  }\n  ctx.closePath();\n}\nfunction draw() {\n  if (reduced) return;\n  ctx.clearRect(0, 0, width, height);\n  const g = ctx.createLinearGradient(0, 0, 0, height);\n  g.addColorStop(0, '#1a1020');\n  g.addColorStop(0.5, '#2d1a35');\n  g.addColorStop(1, '#4a2a3a');\n  ctx.fillStyle = g;\n  ctx.fillRect(0, 0, width, height);\n  for (const s of stars) {\n    const a = 0.3 + 0.5 * Math.sin(t * 0.02 + s.blink);\n    ctx.globalAlpha = a;\n    ctx.fillStyle = '#e8d5a8';\n    ctx.beginPath();\n    ctx.arc(s.x * width, s.y * height, s.r, 0, Math.PI * 2);\n    ctx.fill();\n  }\n  ctx.globalAlpha = 0.9;\n  const venusX = width * 0.75;\n  const venusY = height * 0.25 + Math.sin(t * 0.005) * 20;\n  const venusGlow = ctx.createRadialGradient(venusX, venusY, 0, venusX, venusY, 120);\n  venusGlow.addColorStop(0, 'rgba(255, 200, 120, 0.4)');\n  venusGlow.addColorStop(1, 'rgba(255, 200, 120, 0)');\n  ctx.fillStyle = venusGlow;\n  ctx.beginPath();\n  ctx.arc(venusX, venusY, 120, 0, Math.PI * 2);\n  ctx.fill();\n  ctx.fillStyle = '#ffd700';\n  drawStar(venusX, venusY, 24, 8);\n  ctx.fill();\n  ctx.globalAlpha = 0.15;\n  ctx.strokeStyle = '#c97b8a';\n  ctx.lineWidth = 1;\n  for (const r of rosettes) {\n    const rx = (r.x + Math.sin(t * 0.0003 + r.a) * 0.02) * width;\n    const ry = (r.y + t * 0.0001) % 1.1 * height - 30;\n    ctx.save();\n    ctx.translate(rx, ry);\n    ctx.rotate(r.a + t * 0.001);\n    drawStar(0, 0, 10 * r.s, 8);\n    ctx.stroke();\n    ctx.restore();\n  }\n  ctx.globalAlpha = 0.08;\n  ctx.fillStyle = '#1a0a10';\n  ctx.beginPath();\n  ctx.moveTo(0, height);\n  ctx.lineTo(width * 0.2, height * 0.75);\n  ctx.lineTo(width * 0.4, height * 0.82);\n  ctx.lineTo(width * 0.6, height * 0.7);\n  ctx.lineTo(width * 0.8, height * 0.78);\n  ctx.lineTo(width, height * 0.72);\n  ctx.lineTo(width, height);\n  ctx.fill();\n  ctx.globalAlpha = 1;\n  t++;\n  requestAnimationFrame(draw);\n}\ndraw();"
}
