{
  "canvasId": "anshar-canvas",
  "label": "Whole Heaven",
  "description": "A slowly revolving celestial dome with drifting stars and horizon rings for the primordial sky.",
  "code": "const stars = [];\nfor (let i = 0; i < 100; i++) {\n  stars.push({ a: Math.random() * Math.PI * 2, r: 0.1 + Math.random() * 0.4, s: 0.2 + Math.random() * 1, blink: Math.random() * Math.PI * 2 });\n}\nlet t = 0;\nfunction draw() {\n  if (reduced) return;\n  ctx.clearRect(0, 0, width, height);\n  const g = ctx.createRadialGradient(width * 0.5, height * 0.6, 0, width * 0.5, height * 0.6, width * 0.8);\n  g.addColorStop(0, '#1a2035');\n  g.addColorStop(0.5, '#0f1420');\n  g.addColorStop(1, '#050810');\n  ctx.fillStyle = g;\n  ctx.fillRect(0, 0, width, height);\n  const cx = width * 0.5;\n  const cy = height * 0.6;\n  ctx.strokeStyle = '#3a4a6a';\n  ctx.lineWidth = 1;\n  for (let ring = 1; ring <= 4; ring++) {\n    ctx.globalAlpha = 0.1 / ring;\n    ctx.beginPath();\n    ctx.ellipse(cx, cy, width * 0.15 * ring, height * 0.08 * ring, 0, 0, Math.PI * 2);\n    ctx.stroke();\n  }\n  ctx.globalAlpha = 0.8;\n  ctx.fillStyle = '#d4e0f0';\n  for (const s of stars) {\n    const a = s.a + t * 0.0002 * (s.s + 0.5);\n    const r = s.r * Math.min(width, height);\n    const x = cx + Math.cos(a) * r;\n    const y = cy + Math.sin(a) * r * 0.5;\n    const alpha = 0.3 + 0.5 * Math.sin(t * 0.03 + s.blink);\n    ctx.globalAlpha = alpha;\n    ctx.beginPath();\n    ctx.arc(x, y, s.s, 0, Math.PI * 2);\n    ctx.fill();\n  }\n  ctx.globalAlpha = 0.12;\n  const horizon = ctx.createLinearGradient(0, height * 0.85, 0, height);\n  horizon.addColorStop(0, 'transparent');\n  horizon.addColorStop(1, '#7a8aa8');\n  ctx.fillStyle = horizon;\n  ctx.fillRect(0, height * 0.85, width, height * 0.15);\n  ctx.globalAlpha = 1;\n  t++;\n  requestAnimationFrame(draw);\n}\ndraw();"
}
