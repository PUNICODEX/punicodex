{
  "canvasId": "ninhursag-canvas",
  "label": "Mountain Womb",
  "description": "Rising mountain silhouettes, falling water, and drifting clay particles for the Lady of the Mountain.",
  "code": "const peaks = [];\nfor (let i = 0; i < 5; i++) {\n  peaks.push({ x: (i / 4) * 1.2 - 0.1, w: 0.3 + Math.random() * 0.2, h: 0.3 + Math.random() * 0.25 });\n}\nconst drops = [];\nfor (let i = 0; i < 60; i++) {\n  drops.push({ x: Math.random(), y: Math.random(), s: 0.5 + Math.random() * 1.5, speed: 0.5 + Math.random() });\n}\nconst particles = [];\nfor (let i = 0; i < 40; i++) {\n  particles.push({ x: Math.random(), y: Math.random(), r: 0.5 + Math.random() * 1.5, a: Math.random() * Math.PI * 2 });\n}\nlet t = 0;\nfunction draw() {\n  if (reduced) return;\n  ctx.clearRect(0, 0, width, height);\n  const g = ctx.createLinearGradient(0, 0, 0, height);\n  g.addColorStop(0, '#0f1a14');\n  g.addColorStop(0.5, '#1a2a22');\n  g.addColorStop(1, '#2d3a30');\n  ctx.fillStyle = g;\n  ctx.fillRect(0, 0, width, height);\n  ctx.fillStyle = '#3a4a3a';\n  for (let i = 0; i < peaks.length; i++) {\n    const p = peaks[i];\n    const px = p.x * width;\n    const py = height * 0.7;\n    ctx.beginPath();\n    ctx.moveTo(px, height);\n    ctx.lineTo(px + p.w * width * 0.5, py - p.h * height);\n    ctx.lineTo(px + p.w * width, height);\n    ctx.closePath();\n    ctx.fill();\n  }\n  ctx.globalAlpha = 0.4;\n  ctx.fillStyle = '#7ab8a8';\n  for (const d of drops) {\n    const dy = (d.y + t * 0.001 * d.speed) % 1 * height;\n    ctx.fillRect(d.x * width, dy, 1, d.s * 8);\n  }\n  ctx.globalAlpha = 0.3;\n  ctx.fillStyle = '#a88b6a';\n  for (const p of particles) {\n    const px = (p.x + Math.sin(t * 0.0005 + p.a) * 0.02) * width;\n    const py = (p.y + t * 0.0002) % 1 * height;\n    ctx.beginPath();\n    ctx.arc(px, py, p.r, 0, Math.PI * 2);\n    ctx.fill();\n  }\n  ctx.globalAlpha = 0.15;\n  const glow = ctx.createRadialGradient(width * 0.5, height * 0.35, 0, width * 0.5, height * 0.35, width * 0.4);\n  glow.addColorStop(0, 'rgba(122, 184, 168, 0.3)');\n  glow.addColorStop(1, 'rgba(122, 184, 168, 0)');\n  ctx.fillStyle = glow;\n  ctx.beginPath();\n  ctx.arc(width * 0.5, height * 0.35, width * 0.4, 0, Math.PI * 2);\n  ctx.fill();\n  ctx.globalAlpha = 1;\n  t++;\n  requestAnimationFrame(draw);\n}\ndraw();"
}
